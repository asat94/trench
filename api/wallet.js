import {chainNames,gmgn,unwrap} from './_gmgn.js';
const num=v=>Number(v||0);
const money=v=>{const n=num(v),sign=n<0?'-':'';const a=Math.abs(n);return sign+(a>=1e6?'$'+(a/1e6).toFixed(2)+'M':a>=1e3?'$'+(a/1e3).toFixed(1)+'K':'$'+a.toFixed(0));};
const cache=new Map();
export default async function handler(req,res){res.setHeader('Cache-Control','s-maxage=600, stale-while-revalidate=1800');const chain=['sol','bsc','base','eth','robinhood'].includes(req.query.chain)?req.query.chain:'sol';const address=String(req.query.address||'').trim();if(address.length<20)return res.status(400).json({error:'A valid wallet address is required'});const key=`${chain}:${address}`,cached=cache.get(key);if(cached&&cached.expires>Date.now())return res.status(cached.status).json(cached.body);try{const statsRaw=await gmgn('/v1/user/wallet_stats',{chain,wallet_address:[address],period:'30d'});const stats=Array.isArray(unwrap(statsRaw))?unwrap(statsRaw)[0]:unwrap(statsRaw);const activityRaw=await gmgn('/v1/user/wallet_activity',{chain,wallet_address:address,limit:'12'}).catch(()=>null);const activityData=unwrap(activityRaw)||{};const activities=activityData.activities||activityData.list||[];const common=stats.common||{};const pnlStat=stats.pnl_stat||{};const body={wallet:{address,chain:chainNames[chain],name:common.name||common.ens||common.twitter_username||null,tags:common.tags||common.tag?[...(common.tags||[]),common.tag].filter(Boolean):['Tracked wallet'],realized:money(stats.realized_profit),unrealized:money(stats.unrealized_profit),winRate:(stats.winrate??pnlStat.winrate)!=null?Math.round(num(stats.winrate??pnlStat.winrate)*100)+'%':'—',pnl:(stats.pnl??stats.realized_profit_pnl)!=null?(num(stats.pnl??stats.realized_profit_pnl)*100).toFixed(1)+'%':'—',buys:stats.buy_count??stats.buy_tx_count??'—',sells:stats.sell_count??stats.sell_tx_count??'—',reason:'Listed in the live on-chain wallet activity feed; performance figures cover the last 30 days.'},activities:activities.slice(0,12).map((x,i)=>({id:x.transaction_hash||i,type:String(x.type||x.event_type||'trade').toUpperCase(),symbol:x.token?.symbol||x.symbol||'TOKEN',name:x.token?.name||x.token?.symbol||'Token',amount:money(x.cost_usd),time:x.timestamp?new Date(num(x.timestamp)*(num(x.timestamp)<1e12?1000:1)).toLocaleString():''}))};cache.set(key,{expires:Date.now()+600000,status:200,body});res.status(200).json(body)}catch(e){const body={error:e.message||'Wallet data is temporarily unavailable'};cache.set(key,{expires:Date.now()+60000,status:502,body});res.status(502).json(body)}}
